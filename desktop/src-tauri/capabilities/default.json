{
  "identifier": "default",
  "description": "Default capabilities for Twine Launcher desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled",
    "dialog:allow-open"
  ]
}
